# Articles
    O make Articles clickable
    O create an editor for users to submit requests
    O new Group for Journalists who we trust (they are either the only ones to write or they can acknowledge other peoples articles)
        O add clanker tag on ai generated things
    O Let users take the bullet and enable a self-generate toggle which lets them fetch tagesschau articles, pass them through our ai locally and send them back themself
        O db keeping track of users (with toggle field)
        O on cron trigger somehow check if any user with toggle on is pressent and send a signal; user fetches from tagesschau api and sends the raw article to the server (for if user quits while generating); user generates article and sends it back; we store it with a new tag "user-generated" and let our Journalists check it for errors before it goes public
        O if no users are online (or quit in time) we generate it ourself
# Navbar
    O Make one
    O categories (news, sports, tech, economy, ..., ai|human)
    O dark mode
# More conspiracy
    O make red string between articles
    O make articles look like pinboard entries